<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      .container {
        max-width: 800px;
        margin-top: 50px;
      }
      .feedback-card {
        margin-bottom: 20px;
        padding: 20px;
        border: 1px solid #ddd;
        border-radius: 5px;
      }
      .rating {
        font-weight: bold;
        color: #ffd700;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h3 class="text-center">Admin Dashboard</h3>
      <p class="text-center">View user feedback, traffic, and ratings.</p>

      <% if (users && users.length > 0) { %> <% users.forEach(user => { %>
      <div class="feedback-card">
        <h5><%= user.name %> (<%= user.role %>)</h5>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Visit Count:</strong> <%= user.visitCount %></p>
        <p>
          <strong>Rating:</strong>
          <span class="rating">
            <% for (let i = 0; i < user.rating; i++) { %>★<% } %> <% for (let i
            = user.rating; i < 5; i++) { %>☆<% } %>
          </span>
        </p>
        <p><strong>Feedback:</strong></p>
        <ul>
          <% if (user.feedback && user.feedback.length > 0) { %> <%
          user.feedback.forEach(item => { %>
          <li><%= item.text %></li>
          <% }) %> <% } else { %>
          <li>No feedback available</li>
          <% } %>
        </ul>
      </div>
      <% }) %> <% } else { %>
      <p class="text-center">No users or feedback available.</p>
      <% } %>
    </div>
  </body>
</html>
